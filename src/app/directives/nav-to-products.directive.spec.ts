import { NavToProductsDirective } from './nav-to-products.directive';

describe('NavToProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new NavToProductsDirective();
    expect(directive).toBeTruthy();
  });
});
