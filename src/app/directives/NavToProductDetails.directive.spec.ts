/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NavToProductDetailsDirective } from './NavToProductDetails.directive';

describe('Directive: NavToProductDetails', () => {
  it('should create an instance', () => {
    const directive = new NavToProductDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
